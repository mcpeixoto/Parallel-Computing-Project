# Para compilar
gcc -std=c99 -O2 bucket.c

# Para executar
srun --partition=cpar ./a.out

# Both 
gcc -std=c99 -O2 bucket.c && srun --partition=cpar ./a.out

# Papi
module load papi/5.4.1

# Dar run ao papi (bucket_papi.c)
make clean && make && srun --partition=cpar ./bucket.out